const http=require('http');

const server =http.createServer((req,res)=>{
    if(req.url==='/'){
        res.end('welcome to || the Asknai page') 
    }
if(req.url==='/about'){
    res.end('working on it')
}
    res.end(`<h1>sorry</h1> <p>we cant find page you searching</p><a href="/">back</a>`)
})

server.listen(5000)